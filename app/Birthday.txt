return (
  <>
    {!showWebsite ? (

      <div className="min-h-screen bg-gradient-to-b from-pink-100 via-rose-50 to-pink-200 flex items-center justify-center px-6 relative overflow-hidden">

        {/* Heart Blast */}
        {showBlast && (
          <div className="absolute inset-0 pointer-events-none overflow-hidden z-50">

            {Array.from({ length: 80 }).map((_, i) => (
              <div
                key={i}
                className="absolute text-pink-500 animate-ping"
                style={{
                  left: `${Math.random() * 100}%`,
                  top: `${Math.random() * 100}%`,
                  fontSize: `${20 + Math.random() * 40}px`,
                  animationDuration: `${1 + Math.random() * 2}s`,
                }}
              >
                ❤️
              </div>
            ))}

          </div>
        )}

        <div className="bg-white/30 backdrop-blur-xl border border-white/40 shadow-2xl rounded-[40px] p-10 max-w-xl w-full relative z-10">

          <h1 className="text-5xl font-bold text-center text-rose-500 mb-3">
            Happy Birthday p3  💖
          </h1>

          <p className="text-center text-gray-600 mb-10 text-lg">
            Fill the details and create a magical birthday surprise ✨
          </p>

          <div className="space-y-5">

            <input
              type="text"
              placeholder="Birthday Person Name"
              className="w-full p-4 rounded-2xl border border-pink-200 outline-none focus:ring-4 focus:ring-pink-300 bg-white/70"
            />

            <input
              type="text"
              placeholder="Your Name"
              className="w-full p-4 rounded-2xl border border-pink-200 outline-none focus:ring-4 focus:ring-pink-300 bg-white/70"
            />

            <textarea
              placeholder="Write a lovely birthday message ❤️"
              rows={4}
              className="w-full p-4 rounded-2xl border border-pink-200 outline-none focus:ring-4 focus:ring-pink-300 bg-white/70"
            ></textarea>

            <button
              onClick={handleEnter}
              className="w-full bg-rose-500 hover:bg-rose-600 text-white py-4 rounded-2xl text-xl font-semibold shadow-xl hover:scale-105 transition-all duration-300"
            >
              Enter Love Website 💘
            </button>

          </div>

        </div>

      </div>

          )
    }
  </>
);
}